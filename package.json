{
  "name": "tester1",
  "version": "1.0.0",
  "main": "TPG.js",
  "scripts": {
    
  "build:css": "npx tailwindcss -i ./src/input.css -o ./public/style.css --minify && npx tailwindcss -i ./src/BOK.css -o ./public/BOK.css --minify",
  "build:js": "npx terser ./src/script.js -o ./public/script.js -c -m && npx terser ./src/BOK.js -o ./public/BOK.js -c -m",
  "build:html": "npx html-minifier-terser ./src/index.html -o ./public/index.html --collapse-whitespace --remove-comments --minify-css true --minify-js true && npx html-minifier-terser ./src/BOK.html -o ./public/BOK.html --collapse-whitespace --remove-comments --minify-css true --minify-js true",
  "build": "npm run build:css && npm run build:js && npm run build:html"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@vercel/analytics": "^1.5.0",
    "@vercel/speed-insights": "^1.2.0",
    "esbuild": "^0.25.9"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "cpy-cli": "^6.0.0",
    "html-minifier-terser": "^7.2.0",
    "mkdirp": "^3.0.1",
    "tailwindcss": "^4.1.12",
    "terser": "^5.44.0"
  }
}
