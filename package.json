{
  "name": "tester1",
  "version": "1.0.0",
  "main": "TPG.js",
  "scripts": {
    "buildLocal": "npx tailwindcss -i ./src/input.css -o ./public/output.css --minify",
    "watch": "npx tailwindcss -i ./src/input.css -o ./public/output.css --watch",
    "dev": "npm run watch",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./public/output.css --minify && npx glob './src/!(input).css' -- npx tailwindcss -i {file} -o ./public/{base} --minify",
    "build:js": "npx glob './src/*.js' -- npx terser {file} -o ./public/{base} -c -m",
    "build:html": "npx glob './src/*.html' -- npx html-minifier-terser {file} -o ./public/{base} --collapse-whitespace --remove-comments --minify-css true --minify-js true",
    "build:images": "npx cpx './src/images/**/*' ./public/images",
    "build": "npx mkdirp public && npm run build:css && npm run build:js && npm run build:html && npm run build:images"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@vercel/analytics": "^1.5.0",
    "@vercel/speed-insights": "^1.2.0",
    "esbuild": "^0.25.9"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "mkdirp": "^3.0.1",
    "tailwindcss": "^4.1.12"
  }
}
